<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
<!--        <item name="colorPrimary">@color/colorPrimary</item>-->
<!--        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
<!--        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <!--默认对话框的主题-->
    <style name="DefaultDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--默认加载框的主题-->
    <style name="DefaultDialogStyle.Loading" parent="DefaultDialogStyle">
        <item name="android:backgroundDimEnabled">false</item>
    </style>


    <style name="DefaultToolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/defaultToolBarHeight</item>
        <item name="android:background">@color/colorWhite</item>
        <item name="navigationIcon">@mipmap/ic_nav_back</item>
        <item name="contentInsetStartWithNavigation">0dp</item>
        <item name="contentInsetEndWithActions">0dp</item>
        <item name="android:titleTextAppearance">@style/ToolbarTitle</item>
    </style>
    <style name="ToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
